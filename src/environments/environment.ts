/** Son variables de entorno que se utilizan en el ambiende te producción.
 * No tienen que tener valores correspondientes a entornos localhost.
 */
export const environment = {
    API_URL: "https://agenda-api.somee.com/api/"
};
